# Kernel-specific build settings (build-std, target) are passed via Makefile
# so that `cargo test` and VS Code debug work on the host target without conflicts.

# Default test/check target: same 32-bit word size as the kernel
[build]
target = "i686-unknown-linux-gnu"

# [target.i686-custom]
# rustflags = ["-C", "link-arg=-nostartfiles"]